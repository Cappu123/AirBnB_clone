#!/usr/bin/python3

from models.base_model import BaseModel
class State(BaseModel):
    """
    Representing the state class inheriting from the BaseModel class
    Attributes:
    name(str): an empty string to hold the state name
    """
    name = ""
